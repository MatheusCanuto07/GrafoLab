using System.Reflection.Metadata;

namespace GrafoLab.Representacao
{
  public static class MatrizAdjacencia{
    public static void imprimir(Grafo grafo){
      int tamanho = grafo.listaVertices.Count;
      bool [,] matrizAdjacencia = new bool [tamanho, tamanho];
      for (int i = 0; i < tamanho; i++)
      {
        for (int j = 0; j < tamanho; j++)
        {
          
        }
      }
    }
  }
}